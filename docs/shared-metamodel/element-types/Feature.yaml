element_type: Feature
name_cn: 特性
schema_version: "1.0.0"
common_fields:
  required: [id, element_type, name, status]
  optional: [domain, provenance, lineage, code_anchors, external_refs]
fields:
  - { name: domain, type: string, required: true, relation: part_of, target: Domain, desc: 归属领域 }
  - { name: description, type: string, required: true, desc: 实质描述（避免空壳） }
  - { name: business_value, type: string, required: true, desc: 业务价值 }
relations_held:
  - { relation: part_of, target: Domain, via: domain, seq: 2 }
stage_increments: {}
