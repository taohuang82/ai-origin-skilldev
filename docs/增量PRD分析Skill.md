
本文档是一个 AI Skill 的完整规范，供模型实现时参考。

**Skill 的作用**：当用户提供"业务需求"和"基线 PRD"后，Skill 自动完成影响域分析，输出结构化的改动方案和可执行的 Story 供用户确认，用户审查、修改、批准后产生增量 PRD 文档。

---

## 一、为什么需要这个 Skill（Why）

### 1.1 问题背景

企业 IT 系统在持续迭代优化过程中，每次业务提出新需求，都要回答同一批问题：

- 这个需求影响哪些 PRD 设计要素？
- 哪些要素需要改，改什么？
- 哪些地方绝对不能动？
- 开发怎么拆 Story，怎么验收？

这些问题有规律可循，但如果靠人逐次手动分析，效率低、容易遗漏、结论不一致。

### 1.2 Skill 解决的核心问题

**问题一：影响域不清**
业务说"加一个按钮"，实际上可能影响应用架构、UI 原型、信息架构、功能特性、权限设计等多个 PRD 要素，且这些要素之间存在上下游依赖关系，改了 A 必须同步检查 B。人工分析容易只看到直接影响，漏掉传导影响。

**问题二：改动范围不可控**
在 Coding Agent 参与开发的场景下，如果没有明确的任务边界和禁止项，Agent 可能在分析中自行扩展改动范围，改到不该改的地方。

**问题三：验收标准不可执行**
传统 PRD 的验收标准是给人读的描述，无法直接转化为测试用例，也无法让 Agent 自我核查。

### 1.3 Skill 的价值

通过结构化分析，把"一段业务需求文字"转化成"精确的改动点 + 禁止项 + 可执行的验收标准"，让人只需要做最终确认，而不是从零分析。

---

## 二、整体流程（What）

### 2.1 Skill 的角色定位

```
【输入】
  （A）业务需求 / 业务方案（用户提供）
  基线 PRD（用户提供）

      ↓

【AI Skill 执行，遇到证据不足时立即暂停询问，不猜测】
  Step 1：解析业务需求，识别（B）原子变化场景
  Step 2：通过映射规则，推导（C）主触发 PRD 要素
  Step 3：通过依赖传导规则，推导（D）次要影响要素
  Step 4：对照基线 PRD，确认（E）要素改动点，识别禁止改动项
  Step 5：（F）面向实施，拆分增量 Story，编写结构化验收标准

      ↓

【输出草案，等待人工确认】
  改动点 + 禁止改动项清单 + Story 及验收标准清单

      ↓

【人工确认】
  用户审查、修改、批准

      ↓

【输出】
  增量 PRD 文档
```

分层说明：

- **（A）业务需求 / 业务方案**：输入层，回答"为什么要改、业务想解决什么问题"。
- **（B）原子变化场景**：入口层，回答"产品上到底变了什么"。
- **设计要素层**：承接层，回答"哪些 PRD 章节需要变化"。其中又分两类：
  - **（C）主触发 PRD 要素**：原子场景直接命中的要素。
  - **（D）次要影响要素**：因 PRD 要素间的上下游依赖关系被间接触发的要素。
- **（E）要素改动点**：设计层，回答"每个章节具体怎么改，以及哪些不能改"。
- **（F）面向实施**：收口层，回答"开发怎么落地、验收怎么验证"。

### 2.2 人做什么、AI 做什么

| 环节 | 执行者 | 内容 |
|---|---|---|
| 提供业务需求 | 人 | 业务需求文字、业务方案说明，以及分析过程中的细节澄清 |
| 提供基线 PRD | 人 | 当前系统的完整 PRD 文档 |
| Step 1~5 分析 | AI Skill | 原子场景识别、要素推导、改动点/禁止项分析、Story 拆分/AC 编写 |
| 审查草案 | 人 | 检查分析是否准确、是否有遗漏或错误 |
| 确认 / 修改 / 批准 | 人 | 对结果负责的节点 |
| 增量 PRD 文档 | AI Skill | 按确认结论结构化输出 |

---

## 三、两条铁律

这两条规则优先级高于一切，在执行任何 Step 时均须严格遵守。

### 铁律一：有理有据，不猜测

**每一条分析结论都必须有明确的依据来源。** 依据只有两种：

- **来自业务需求原文**：直接引用原文片段作为证据
- **来自基线 PRD**：明确指出基线 PRD 的章节名、表格行或字段名

**当依据不足时，唯一合法的处理方式是：暂停并询问用户，而不是推断或假设。**

具体表现：
- 业务需求描述了"录入供应商信息"，但没说具体录哪些字段 → 不能自行推断字段，必须询问
- 基线 PRD 的权限矩阵章节缺失 → 不能假设权限规则，必须询问
- 需求中提到"历史数据需要处理"，但没说怎么处理 → 不能自行设计迁移策略，必须询问

### 铁律二：随时暂停，主动澄清

**分析过程不是一次性的线性执行，而是允许在任何步骤中途暂停询问。** 以下情况必须暂停：

| 触发情形 | 暂停时机 | 示例 |
|---|---|---|
| 业务需求有歧义，同一段描述可以对应多个不同的原子场景 | Step 1 识别时 | "优化供应商选择"——是新增页面、还是现有页面加字段，无法判断 |
| 原子场景识别置信度为"低" | Step 1 识别后 | 需求只说"改善体验"，没有具体操作描述 |
| "需验证"要素在基线 PRD 中找不到对应章节 | Step 2/3 推导时 | 基线 PRD 无权限设计章节，无法判断是否影响权限 |
| 改动点的 target_state 无法从需求中确定 | Step 4 分析时 | 需求说"加字段"但没说字段名、类型、必填性 |
| 历史数据处理策略不明确 | Step 4 分析时 | 需求未说明存量数据如何处理 |
| 禁止改动项依赖外部系统知识，PRD 中无记录 | Step 4 分析时 | 某接口是否有外部调用方，PRD 中未标注 |
| 基线 PRD 中存在相互矛盾的内容 | 任意步骤 | 功能特性描述和信息架构字段定义不一致 |

**暂停和询问的格式要求**：

```
⏸ 分析暂停 — 需要澄清以下问题，才能继续执行 [Step X / 要素名]：

Q1：[具体问题]
背景：[为什么需要这个信息，不澄清会导致什么无法判断]

Q2：[具体问题]
背景：[为什么需要这个信息]

请回答以上问题后，我将继续分析。
```

**要求**：
- 同一次暂停中的问题必须集中一次性提出，不要逐问逐答（太碎片化）
- 每个问题必须说明"背景"，让用户理解为什么这个问题是必须回答的
- 问题应具体，不能是泛化的"请补充更多信息"
- 收到回答后，从暂停的步骤继续执行，并在输出中注明"根据用户澄清：[关键信息]"

---

## 四、关键概念定义（What）

Skill 执行过程中会产生以下结构化对象，实现时严格按这些格式输出。

### 4.1 原子变化场景（AtomicScenario）

```
AtomicScenario = {
  id:            场景编号（如 UI-01）,
  name:          场景名称,
  evidence:      从需求原文中提取的依据（直接引用原文片段，不能改写）,
  confidence:    高 / 中 / 低（识别置信度）,
  open_question: 置信度为中/低时的待确认问题（置信度高时可空）
}
```

### 4.2 改动点（ChangePoint）

```
ChangePoint = {
  id:              改动点编号（如 CP-001）,
  source_scenario: 来源原子场景编号,
  trigger_type:    主触发 / 依赖传导（注明传导规则编号，如 T-01）,
  element:         受影响的 PRD 要素名称,
  action:          新增 / 修改 / 删除 / 复用 / 不涉及,
  baseline_ref:    基线 PRD 对应章节名或内容（必须精确引用，不能空缺）,
  baseline_state:  基线现状描述（直接引用基线内容，或说明"基线无此内容"）,
  target_state:    变更后目标状态描述（必须有需求原文或澄清回答作为依据）,
  in_scope:        本改动明确包含的对象列表,
  out_of_scope:    本改动明确排除的对象列表
}
```

### 4.3 禁止改动项（ForbiddenItem）

禁止改动项是独立列表，与改动点并列输出，不内嵌在 ChangePoint 中。

```
ForbiddenItem = {
  id:           禁止项编号（如 FB-001）,
  target:       禁止改动的具体对象（实体名 / 字段名 / 接口名 / 页面名）,
  reason:       禁止原因（现有依赖 / 合规约束 / 架构边界 / 历史数据风险）,
  consequence:  若违反此项会发生什么,
  adjacent_to:  与哪些改动点相邻（最容易在这些改动旁越界）,
  evidence:     依据来源（基线 PRD 章节 / 用户澄清说明）
}
```

**注意**：禁止改动项与改动点是并列关系，都在草案中独立输出。在 Story 的"改动范围→不涉及"中引用相关禁止项编号。

### 4.4 验收标准（AcceptanceCriteria）

```
AcceptanceCriteria = {
  id:        验收标准编号（如 AC-001）,
  cp_ref:    关联改动点编号,
  type:      功能验证 / 数据验证 / 权限验证 / 集成验证 / 边界验证,
  given:     前置条件（系统处于什么状态，哪些数据已存在）,
  when:      触发动作（执行什么操作）,
  then:      期望结果（必须可以被精确断言，不能有主观词汇）,
  negative:  反例（哪种情况应该不通过，可空）
}
```

**then 的写法要求**：

正确示例：
- "页面显示字段`供应商名称`，值与实体 Supplier.name 一致"
- "返回 HTTP 200，响应体包含字段 order_no，类型为 string，不为空"
- "角色`采购员`看不到按钮`审批通过`，DOM 中不存在该元素"
- "数据库表 purchase_order 中存在字段 supplier_code，类型为 varchar(50)，可为空"

错误示例（不可接受）：
- "界面展示正确"（无法断言）
- "系统正常运行"（无法断言）
- "用户体验良好"（主观，无法断言）

---

## 五、执行步骤详解（How）

以下是 Skill 的完整执行逻辑。每一步有明确的输入、处理方法、暂停触发条件和输出。

---

### Step 1：解析业务需求，识别原子变化场景

**输入**：业务需求文字 / 业务方案

**处理方法**：

逐句阅读需求，寻找变化信号，将每个信号映射到以下原子场景清单中的对应类型，并直接引用触发该判断的需求原文作为 evidence。

**原子场景完整清单**（所有合法的原子场景均在此列，如遇清单外的变化类型须在输出中注明并暂停询问）：

*UI 类——交互层变化*

| 编号 | 名称 | 识别关键词 |
|---|---|---|
| UI-01 | 操作入口新增 | 新增按钮、新增菜单项、新增 Tab、新增操作链接 |
| UI-02 | 新增页面 / 弹窗 | 新增页面、新增弹窗、新增抽屉、新增表单面板 |
| UI-03 | 已有页面新增字段展示 | 列表加字段、详情页加字段、表格加列 |
| UI-04 | 字段编辑能力变化 | 只读变可编辑、加必填校验、取消必填 |
| UI-05 | 界面流程步骤调整 | 新增审批步骤、跳过某步骤、调整提交流程 |
| UI-06 | 字段显隐 / 联动规则变化 | 条件显示字段、联动带出值、动态必填 |
| UI-07 | 操作入口变更 / 移除 | 按钮改名、按钮灰化条件变化、移除按钮 |
| UI-08 | 页面 / 模块整体删除 | 下线某功能页面、移除某模块 |

*DA 类——数据层变化*

| 编号 | 名称 | 识别关键词 |
|---|---|---|
| DA-01 | 现有实体新增字段 | 实体加字段、表加列、对象加属性 |
| DA-02 | 字段约束 / 类型变化 | 改字段长度、改枚举值、加默认值、改必填性 |
| DA-03 | 字段语义 / 赋值规则变化 | 字段含义调整、计算方式变化、来源变化 |
| DA-04 | 新增独立实体 | 新增业务对象、新增数据表、新增逻辑实体 |
| DA-05 | 实体关系变化 | 实体间关联关系调整、1:N 变 N:M |
| DA-06 | 实体状态流转变化 | 新增状态、删除状态、状态转换条件变化 |
| DA-07 | 历史数据处理 | 历史数据补填、数据迁移、存量数据默认值 |
| DA-08 | 数据删除 / 归档规则 | 数据保留策略、清理规则、归档触发条件 |

*LG 类——逻辑层变化*

| 编号 | 名称 | 识别关键词 |
|---|---|---|
| LG-01 | 新增独立功能模块 | 新增一个完整功能、新增一个独立能力 |
| LG-02 | 现有功能规则变化 | 校验规则变化、计算逻辑变化、处理方式变化 |
| LG-03 | 权限规则变化 | 角色可见性变化、操作权限变化、数据权限变化 |
| LG-04 | 通知 / 消息规则变化 | 新增通知、修改通知触发条件、修改通知内容 |
| LG-05 | 批量能力新增 | 批量导入、批量导出、批量处理、批量删除 |
| LG-06 | 配置项变化 | 新增可配置项、修改配置项、删除配置项 |

*PR 类——流程层变化*

| 编号 | 名称 | 识别关键词 |
|---|---|---|
| PR-01 | 新增端到端场景 | 新增业务流程、新增完整操作链路 |
| PR-02 | 现有场景步骤变化 | 流程步骤增减、主流程调整、异常分支变化 |
| PR-03 | 场景角色职责变化 | 某步骤执行者变化、审批人变化 |

*IN 类——集成层变化*

| 编号 | 名称 | 识别关键词 |
|---|---|---|
| IN-01 | 新增外部系统集成 | 对接新系统、新增接口、新增数据同步 |
| IN-02 | 现有接口契约变化 | 接口字段变化、入参出参调整、SLA 变化 |
| IN-03 | 集成移除 | 停用某接口、断开某外部系统依赖 |

*NF 类——质量层变化*

| 编号 | 名称 | 识别关键词 |
|---|---|---|
| NF-01 | 性能 / 并发指标变化 | 响应时间要求、并发量、数据量要求 |
| NF-02 | 安全 / 合规变化 | 数据脱敏、加密、审计日志、合规约束 |
| NF-03 | 可用性 / 容灾变化 | SLA、故障恢复、降级策略 |
| NF-04 | 埋点 / 监控变化 | 新增埋点、修改监控指标 |
| NF-05 | 可维护性变化 | 日志要求、运维操作、诊断能力 |

**Step 1 暂停触发条件**：

- 同一段需求文字可以合理对应两个以上不同原子场景，无法区分 → 暂停询问
- 识别出的场景置信度为"低" → 暂停询问（可与其他低置信度问题合并后一次性询问）
- 需求中有明确变化信号但场景类型超出清单范围 → 暂停询问

**输出**：AtomicScenario 列表

---

### Step 2：推导主触发 PRD 要素

**输入**：Step 1 的原子场景列表

**处理方法**：

对每个原子场景，查询以下映射表，得到"必然影响"和"需验证"的 PRD 要素。每条推导结论都以来源场景编号作为依据。

**场景 → 要素映射表**：

| 原子场景 | 必然影响的要素 | 需验证的要素 |
|---|---|---|
| UI-01 | 界面原型、功能特性 | 权限设计、场景解决方案 |
| UI-02 | 界面原型、功能特性 | 权限设计、信息架构、场景解决方案 |
| UI-03 | 界面原型、功能特性 | 信息架构 |
| UI-04 | 界面原型、功能特性 | 权限设计、信息架构 |
| UI-05 | 界面原型、功能特性、场景解决方案 | 权限设计 |
| UI-06 | 界面原型、功能特性 | 信息架构 |
| UI-07 | 界面原型、功能特性 | 权限设计、场景解决方案 |
| UI-08 | 界面原型 | 应用架构、权限设计、场景解决方案 |
| DA-01 | 信息架构、功能特性 | 界面原型、集成设计 |
| DA-02 | 信息架构、功能特性 | 界面原型、集成设计 |
| DA-03 | 信息架构、功能特性 | 场景解决方案、集成设计 |
| DA-04 | 信息架构、功能特性、场景解决方案 | 权限设计、集成设计 |
| DA-05 | 信息架构、功能特性 | 场景解决方案 |
| DA-06 | 信息架构、功能特性、场景解决方案 | 界面原型 |
| DA-07 | （无必然影响要素，全部需验证） | 信息架构、功能特性、配置设计、非功能需求 |
| DA-08 | 信息架构 | 功能特性、配置设计、非功能需求 |
| LG-01 | 应用架构、功能特性、权限设计、场景解决方案 | 界面原型、信息架构、集成设计、配置设计 |
| LG-02 | 功能特性 | 应用架构、界面原型、权限设计、场景解决方案 |
| LG-03 | 权限设计 | 功能特性、场景解决方案、非功能需求 |
| LG-04 | 功能特性 | 集成设计、配置设计 |
| LG-05 | 界面原型、功能特性 | 应用架构、权限设计、非功能需求 |
| LG-06 | 配置设计 | 功能特性、非功能需求 |
| PR-01 | 场景解决方案 | 应用架构、功能特性、权限设计、集成设计 |
| PR-02 | 场景解决方案 | 功能特性、权限设计、集成设计 |
| PR-03 | 权限设计、场景解决方案 | 功能特性 |
| IN-01 | 应用架构、集成设计 | 信息架构、功能特性、场景解决方案、非功能需求 |
| IN-02 | 集成设计 | 信息架构、功能特性、非功能需求 |
| IN-03 | 集成设计 | 应用架构、功能特性 |
| NF-01 | 非功能需求 | 应用架构、集成设计 |
| NF-02 | 非功能需求 | 权限设计、信息架构、集成设计 |
| NF-03 | 非功能需求 | 应用架构、集成设计、配置设计 |
| NF-04 | 非功能需求 | 功能特性 |
| NF-05 | 非功能需求 | 配置设计 |

注意：Story 设计是所有改动的最终收口，每个有效改动点都会生成对应 Story，在此不单独列入映射表。

**Step 2 暂停触发条件**：

"需验证"要素需要在基线 PRD 中查找对应章节。若基线 PRD 中完全没有该要素的章节（如无权限设计章节），无法判断是否影响 → 暂停询问。

**输出**：每个要素对应"必然影响 / 需验证 / 不影响"的初始判断，带上来源场景编号

---

### Step 3：执行依赖传导规则

**输入**：Step 2 的要素初始判断结果

**处理目的**：PRD 各要素之间存在上下游依赖关系。当某个要素发生"新增"类变化时，其上游来源要素也可能需要同步更新。Step 2 的映射表只覆盖了"原子场景 → 要素"的直接关系，这一步补充"要素 → 上游要素"的间接关系。

**处理方法**：

遍历 Step 2 的结果，逐条检查以下传导规则。凡命中规则的，将对应要素加入受影响清单，标注 trigger_type 为"依赖传导（规则 T-xx）"。

**传导规则 T-01：功能特性新增 → 应用架构必然影响**
- 触发条件：任意改动点的动作类型为"功能特性：新增"
- 原因：每个功能特性必须挂靠在应用架构的某个子特性节点下。新增功能特性意味着要么新增子特性，要么扩展现有子特性的范围，两种情况都涉及应用架构。
- 后续处理：在 Step 4 中回答——基线是否有子特性可承接本次新增的功能特性？有则应用架构"修改"，无则应用架构"新增子特性"。

**传导规则 T-02：功能特性.实体操作说明变化 → 信息架构需验证**
- 触发条件：功能特性的实体操作说明有新增或修改
- 原因：实体操作说明描述的是对信息架构中实体的 CRUD 操作，操作说明变化可能意味着字段或关系需要同步调整。
- 后续处理：检查操作涉及的字段和关系是否已在基线实体中存在。

**传导规则 T-03：场景解决方案新增 → 应用架构需验证**
- 触发条件：场景解决方案中新增了场景或步骤
- 原因：场景步骤需要子特性作为执行载体，新场景可能引用了尚未在架构中定义的子特性。
- 后续处理：检查新场景每个步骤是否有对应子特性承接。

**传导规则 T-04：新增页面 → 信息架构需验证**
- 触发条件：界面原型新增了独立页面（UI-02 触发）
- 原因：新页面展示或录入的字段必须有数据来源，需确认字段是否已在基线实体中存在。
- 后续处理：对页面上每个字段，在基线实体中核查是否已有对应字段。

**传导规则 T-05：新增页面 → Pageflow 必然影响**
- 触发条件：界面原型新增了独立页面（UI-02 触发）
- 原因：新页面必须有入口（从哪里跳进来）和出口（完成后去哪里），否则用户无法访问。
- 后续处理：明确新页面的入口触发点和操作完成后的跳转目标，更新 Pageflow。

**传导规则 T-06：页面删除 → 应用架构需验证**
- 触发条件：界面原型有页面整体删除（UI-08 触发）
- 原因：若被删页面是某个子特性的唯一 UI 载体，则该子特性在产品上实际已废弃，应用架构子特性树需同步更新。
- 后续处理：检查被删页面对应的子特性是否还有其他 UI 入口，如无则标记废弃。

**输出**：补充了传导触发要素的完整受影响要素清单，每个要素标注触发类型

---

### Step 4：对照基线 PRD 确认改动点，识别禁止改动项

**输入**：Step 3 的受影响要素清单 + 基线 PRD 文档

**处理方法**：

对每个受影响要素，在基线 PRD 中找到对应章节，阅读现有内容，分析本次变化需要改什么，生成 ChangePoint 对象；同时识别每个改动点相邻的禁止改动项，生成 ForbiddenItem 对象（独立列表）。

**"需验证"要素的处理**：

对 Step 2/3 中标注为"需验证"的要素，执行以下检查，根据结论决定保留或移除：

| 要素 | 关键检查问题 | 依据来源 |
|---|---|---|
| 信息架构 | 涉及的字段是否已在基线实体中存在？实体是否已有？ | 基线 PRD 实体详情章节 |
| 应用架构 | 是否有现有子特性可以承接新功能？是否有子特性需要废弃？ | 基线 PRD 子特性清单 |
| 权限设计 | 是否新增了角色可见 / 可操作的功能点？数据权限范围是否变化？ | 基线 PRD 权限矩阵 |
| 场景解决方案 | 端到端业务链路是否发生变化？ | 基线 PRD 场景清单 |
| 集成设计 | 是否涉及外部系统？是否改变了对外契约？ | 基线 PRD 集成点说明 |
| 配置设计 | 是否有参数需要运行时可配置？ | 业务需求描述 |
| 非功能需求 | 是否引入新的性能、安全、隐私约束？ | 业务需求描述 |

**防过度设计检查**（避免把小需求分析成大改动）：

- 新增按钮 ≠ 必然新增子特性（需判断是否有独立业务边界）
- 新增页面 ≠ 必然新增实体（页面可能展示已有实体数据）
- 表格加展示字段 ≠ 必然修改信息架构（字段可能已有）
- 字段入库 ≠ 必然新增端到端场景
- 新字段 ≠ 必然修改集成设计（只有需要对外暴露时才影响）

**每个 PRD 要素的改动分析要点**：

**应用架构**
- 关键问题：新功能挂靠哪个子特性？是否需要新增子特性节点？是否有子特性需要废弃？
- 分析内容：子特性新增、范围扩展、废弃标记
- 常见动作：修改（扩展现有子特性范围）/ 新增（添加新子特性节点）/ 废弃

**界面原型**
- 关键问题：哪个页面改了什么控件？新页面的入口和出口是什么？
- 分析内容：页面、按钮、字段、弹窗、Pageflow 的具体变化
- 注意：新增页面时必须同步更新 Pageflow（T-05 规则）

**信息架构**
- 关键问题：字段是否已存在？实体是否已存在？历史数据如何处理？
- 分析内容：字段定义（名称、类型、必填性、枚举值）、实体结构、ER 关系、数据样例
- 注意：新增字段必须说明历史数据的处理策略（默认值或迁移方案）

**功能特性**
- 关键问题：是新增功能特性还是修改已有功能特性？操作步骤、业务规则、实体操作说明是否变化？
- 分析内容：UIUX 操作说明、实体操作说明、业务规则、校验逻辑、异常处理
- 注意：功能特性"新增"时必须触发 T-01 检查

**权限设计**
- 关键问题：谁能看这个功能？谁能操作？数据权限范围是否变化？
- 分析内容：角色权限矩阵、按钮权限、数据可见范围的具体变化

**集成设计**
- 关键问题：是否涉及外部系统？接口入参或出参是否变化？
- 分析内容：接口字段、数据映射、调用时机、异常处理

**配置设计**
- 关键问题：业务规则是否需要参数化？是否有运行时可调整的配置项？
- 分析内容：配置项名称、类型、默认值、作用域

**场景解决方案**
- 关键问题：端到端业务链路是否变化？是否新增场景或修改现有场景步骤？
- 分析内容：场景名称、步骤描述、角色、数据流向

**非功能需求**
- 关键问题：是否引入新的性能、安全、隐私、审计要求？
- 分析内容：具体指标或约束的变化

**禁止改动项的识别范围**：

每个改动点分析完成后，检查以下类型的对象是否需要列为禁止项：

1. **现有接口契约**：本次改动涉及接口，但接口已有其他调用方（需依据：基线 PRD 集成点说明中有记录）
2. **实体主键和唯一约束**：本次改动涉及的实体中，主键和业务唯一键禁止修改
3. **外部系统依赖字段**：若某字段有外部系统订阅（需依据：基线 PRD 集成设计中有记录，否则须询问用户）
4. **范围外的相邻代码区域**：改动点 in_scope 之外但物理临近的功能区域
5. **架构边界**：本次明确不涉及的模块、服务、表

**Step 4 暂停触发条件**：

- ChangePoint 的 target_state 无法从需求原文中确定（如需求只说"加字段"但未说字段名、类型、必填性）→ 暂停询问
- 历史数据处理策略不明确 → 暂停询问
- 禁止改动项需要外部系统依赖知识，但基线 PRD 无记录 → 暂停询问
- 信息架构字段已存在但定义与需求描述有语义出入 → 暂停询问

**输出**：ChangePoint 列表（含 baseline_state 和 target_state）+ ForbiddenItem 独立列表

---

### Step 5：拆分增量 Story，编写结构化验收标准

**输入**：Step 4 的 ChangePoint 列表

**处理方法**：

按 INVEST 原则拆分 Story：每个 Story 应满足——
- **I（Independent）**：Story 之间尽量相互独立，可以单独开发和验收
- **N（Negotiable）**：Story 描述的是"做什么"而非"怎么做"
- **V（Valuable）**：每个 Story 对用户或系统有可感知的价值
- **E（Estimable）**：Story 粒度应可以被评估工作量（通常1-3天）
- **S（Small）**：一个 Story 不超过一个人一个迭代完成
- **T（Testable）**：有明确可验证的验收标准

**验收标准必须覆盖的类型**：

- 所有"界面原型"改动：至少一条功能验证（字段正确展示）
- 所有"信息架构"改动：至少一条数据验证（字段正确存储）
- 所有"权限设计"改动：至少一条权限验证（角色可见性正确）
- 所有"集成设计"改动：至少一条集成验证（接口返回正确）
- 涉及边界条件（必填、长度限制、枚举值）的改动：至少一条边界验证

**then 编写原则**：
- 描述系统的可观测状态，而非操作过程
- 精确到字段名、状态值、HTTP 状态码等可断言的具体值
- 不使用"正确"、"成功"、"正常"等主观词汇，除非紧跟具体可验证的状态

**Story 格式**：

```markdown
## Story [编号]：[Story 名称]

**来源**：ChangePoint [CP-xxx]，场景 [UI-xx / DA-xx ...]

**用户故事**：
作为 [角色]，我希望 [做什么]，以便 [达到什么目的]。

**改动范围**：
- 涉及：[明确列出改动对象]
- 不涉及：[引用相关 ForbiddenItem 编号及对象名]

**详细说明**：
[基于 ChangePoint 的 target_state 展开描述]

**验收标准**：
- AC-[编号]（[类型]）
  - Given：[前置条件]
  - When：[触发动作]
  - Then：[期望结果]
  - Negative：[反例，可空]

**关联 PRD 章节**：[对应 PRD 要素章节引用]
```

**输出**：增量 Story 列表

---

## 六、人工确认环节

Skill 在完成 Step 1-5 后，按以下格式输出草案，然后停止，等待用户确认，不自动继续执行：

```
=== 增量 PRD 影响域分析草案 ===

【一、原子变化场景】
（AtomicScenario 列表，含 evidence 和 open_question）

【二、受影响 PRD 要素总表】
要素名 | 触发类型（主触发/依赖传导/规则编号）| 动作 | 改动摘要

【三、不涉及要素说明】
要素名 | 不涉及原因 | 验证依据（引用基线 PRD 章节）

【四、改动点明细】
（ChangePoint 列表，含 baseline_state、target_state、in_scope、out_of_scope）

【五、禁止改动项清单】
（ForbiddenItem 独立列表）

【六、增量 Story 及验收标准】
（Story 列表，每个 Story 含完整 AC）

=== 待确认问题汇总 ===
（分析过程中未暂停询问但仍存在的低置信度判断，集中列出）

---
以上为本次分析草案，请确认：
1. 分析结论是否准确，有无遗漏或错误？
2. 禁止改动项是否完整，有无需要补充的约束？
3. Story 拆分粒度是否合适？
确认后我将生成增量 PRD 文档。如有修改意见请直接说明，我将按修改意见调整后重新输出。
```

---

## 七、最终交付物生成

用户确认（含任何修改后的最终确认）后，生成增量 PRD 文档。

**增量 PRD 文档结构**：

```markdown
# 增量 PRD — [需求名称]

## 0. 变更说明
- 需求来源：[业务需求编号 / 名称]
- 分析日期：[日期]
- 确认人：[用户姓名]
- 本次变更范围摘要：[一段话概括]

## 1. 原子变化场景清单
（最终确认的 AtomicScenario 列表）

## 2. 受影响要素总表
（最终确认的要素影响清单）

## 3. 不涉及要素说明
（明确排除的要素及原因）

## 4. 各要素增量改动详情
（按 PRD 要素逐一展开，每个要素格式如下）

### [要素名称]
- 触发类型：主触发 / 依赖传导（规则 T-xx）
- 动作：新增 / 修改 / 删除
- 基线章节：[引用]
- 改动内容：
  [具体改动描述]
- 禁止改动项：[引用相关 FB-xxx]

## 5. 禁止改动项清单
（ForbiddenItem 列表）

## 6. 增量 Story 列表
（最终确认的 Story 列表，含完整 AC）
```

---

## 八、PRD 要素参考清单

| PRD 要素 | 包含的子要素 |
|---|---|
| 定位与目标 | 用户和干系人、痛点和价值、范围和边界、产品目标 |
| 应用架构 | 应用架构图、系统边界、特性分类 / 特性 / 子特性 |
| 界面原型 | UI 页面 / 卡片、菜单、Pageflow、兼容性设计、交互原型 |
| 信息架构 | 业务对象 / 逻辑实体、实体关系图、实体详情、实体状态流转、数据样例、历史数据切换 |
| 功能特性 | 子特性基本信息、特性分类、UIUX 操作说明、实体操作说明、集成点说明、业务规则、验收标准 |
| 权限设计 | 角色定义、功能权限矩阵、数据权限规则 |
| 集成设计 | 集成点、API、MQ、数据集成 |
| 配置设计 | 用户配置项、IT 管理配置项 |
| 场景解决方案 | 场景清单、解决方案集成图 |
| 非功能需求 | 性能要求、埋点要求、安全隐私、易用性、可维护性 |
| Story 设计 | Story 清单、Story 概述、Story 描述、验收标准、关联详细设计 |

---

## 九、完整执行案例

以下两个案例完整演示了 Skill 的执行过程，包括澄清暂停的触发和处理方式。

---

### 案例 A：业务需求信息不完整，需要多次澄清

**场景背景**：用户提交如下业务需求，并附上了基线 PRD。

> **业务需求原文**：
> "在采购单页面增加供应商指定功能，采购员可以录入供应商相关信息并保存。历史采购单也需要处理。"

**基线 PRD 摘录**（用户提供，以下是与本次分析相关的部分）：
> - 应用架构 §2.3：子特性清单包含"采购单创建"、"采购单查询"、"采购单审批"，无"供应商指定"相关子特性
> - 界面原型 §3.1：采购单列表页，列含：采购单号、状态、金额、创建人、创建时间；操作列含：查看、编辑、删除
> - 信息架构 §4.2：PurchaseOrder 实体字段：order_no（主键）、status、amount、creator_id、created_at
> - 权限设计 §6.1：角色清单包含采购员、采购主管、财务；**权限矩阵章节标注"待完善"**
> - 集成设计 §7.1：采购单列表接口 GET /api/purchase-orders，供外部报表系统调用，出参包含全部 PurchaseOrder 字段

---

**🤖 Skill 执行开始**

**Step 1：识别原子变化场景**

阅读需求原文，提取变化信号：

- "增加供应商指定功能" → 有"增加功能"信号，但不清楚是新增按钮进入弹窗，还是在现有编辑页直接加字段，两种解读对应不同原子场景。→ 置信度：低
- "录入供应商相关信息" → 有录入动作，说明存在录入界面，但字段不明确。→ 信息不足
- "历史采购单也需要处理" → 对应 DA-07（历史数据处理），置信度高，但处理方式不明确。

识别到低置信度问题，触发暂停。

---

> ⏸ **分析暂停 — 需要澄清以下问题，才能继续 Step 1**
>
> **Q1：供应商指定功能的入口形式是什么？**
> 背景：需求说"增加供应商指定功能"，可以理解为（a）在采购单列表页加一个"指定供应商"按钮，点击后弹窗录入；或（b）在采购单编辑页直接新增供应商相关字段。两种方式对应不同的原子场景，分析结论不同，无法推断。
>
> **Q2：需要录入哪些供应商信息？请列出字段名称、类型和必填性。**
> 背景：需求提到"录入供应商相关信息"，但未说明具体字段。字段定义直接决定信息架构是否需要变更，以及变更的具体内容，无法推断。
>
> **Q3：历史采购单如何处理？**
> 背景：需求说"历史采购单也需要处理"，可以理解为（a）历史数据新字段默认填空值；（b）需要人工补填；（c）通过脚本批量迁移。三种处理方式的实施成本和风险差异很大，需要业务方明确。
>
> 请回答以上问题后，我将继续分析。

---

**用户回答**：
> Q1：在列表页操作列加一个"指定供应商"按钮，点击后弹窗录入。
> Q2：字段是：供应商编码（varchar 50，必填）、供应商备注（varchar 200，选填）。
> Q3：历史数据两个字段默认 null，不需要补填。

---

**🤖 根据用户澄清继续 Step 1**

根据用户澄清（Q1：按钮+弹窗形式；Q2：supplier_code、supplier_remark；Q3：历史数据默认 null），完成原子场景识别：

| id | name | evidence | confidence | open_question |
|---|---|---|---|---|
| UI-01 | 操作入口新增 | 原文"增加供应商指定功能"+ 用户澄清"列表页操作列加按钮" | 高 | 无 |
| UI-02 | 新增弹窗 | 用户澄清"点击后弹窗录入" | 高 | 无 |
| DA-01 | 现有实体新增字段 | 用户澄清"供应商编码（varchar 50，必填）、供应商备注（varchar 200，选填）" | 高 | 无 |
| DA-07 | 历史数据处理 | 原文"历史采购单也需要处理"+ 用户澄清"默认 null" | 高 | 无 |

---

**Step 2：推导主触发 PRD 要素**

查询映射表，依据各原子场景编号：

| 要素 | 判断 | 来源场景 |
|---|---|---|
| 界面原型 | 必然影响 | UI-01、UI-02 |
| 功能特性 | 必然影响 | UI-01、UI-02、DA-01 |
| 信息架构 | 必然影响 | DA-01；DA-07 需验证 |
| 权限设计 | 需验证 | UI-01（映射表） |
| 场景解决方案 | 需验证 | UI-02（映射表） |
| 集成设计 | 需验证 | DA-01（映射表） |

---

**Step 3：执行依赖传导**

遍历 Step 2 结果：

- 功能特性预期为"新增"（新增"指定供应商"操作） → 触发 **T-01**：应用架构加入必然影响
- UI-02 新增弹窗 → 触发 **T-04**：信息架构需验证（弹窗字段是否在基线实体中已有）
- UI-02 新增弹窗 → 触发 **T-05**：Pageflow 必然影响（需定义按钮触发弹窗路径）

补充后完整受影响要素清单：

| 要素 | 判断 | 触发类型 |
|---|---|---|
| 应用架构 | 必然影响 | 依赖传导 T-01 |
| 界面原型（含 Pageflow） | 必然影响 | 主触发 UI-01、UI-02；传导 T-05 |
| 信息架构 | 必然影响（DA-01 直接触发）+ 需验证（T-04 传导） | 主触发 DA-01；传导 T-04 |
| 功能特性 | 必然影响 | 主触发 UI-01、UI-02、DA-01 |
| 权限设计 | 需验证 | 主触发 UI-01 |
| 场景解决方案 | 需验证 | 主触发 UI-02 |
| 集成设计 | 需验证 | 主触发 DA-01 |

---

**Step 4：对照基线 PRD 确认改动点**

**4a. 处理"需验证"要素**

**权限设计**：查基线 PRD §6.1，权限矩阵章节标注"待完善"，无法判断"指定供应商"按钮是否所有角色可见，无法判断是否影响。触发暂停。

> ⏸ **分析暂停 — 需要澄清以下问题，才能继续 Step 4（权限设计）**
>
> **Q4："指定供应商"按钮对哪些角色可见、可操作？**
> 背景：基线 PRD §6.1 权限矩阵章节标注"待完善"，无法从基线中判断现有角色的权限规则。需求中未说明按钮的权限控制。当前系统有采购员、采购主管、财务三个角色，请明确哪些角色可以看到并使用此按钮。
>
> 请回答后，我将继续完成权限设计部分分析。

---

**用户回答**：
> 只有采购员可以看到和使用"指定供应商"按钮，采购主管和财务不显示此按钮。

---

**🤖 根据用户澄清继续 Step 4（权限设计）**

根据用户澄清（Q4），权限设计影响，判断为"修改"。

**场景解决方案**：查基线 PRD 场景清单，无"供应商指定"相关端到端场景。新弹窗录入是在列表页完成的单步操作，不涉及跨角色协作或多步骤流程，判断**不新增场景**。场景解决方案**不涉及**。依据：基线无相关场景且操作为单步独立动作，不改变端到端链路。

**集成设计**：查基线 PRD §7.1，采购单列表接口 GET /api/purchase-orders 供外部报表系统调用，出参包含全部 PurchaseOrder 字段。新增 supplier_code 和 supplier_remark 两个字段后，如果接口出参包含"全部字段"，则新字段会自动出现在出参中，即接口契约发生变化（外部报表系统会收到新字段）。需确认。触发暂停。

> ⏸ **分析暂停 — 需要澄清以下问题，才能继续 Step 4（集成设计）**
>
> **Q5：外部报表系统对采购单接口新增字段是否有影响？**
> 背景：基线 PRD §7.1 记录 GET /api/purchase-orders 接口供外部报表系统调用，出参包含"全部 PurchaseOrder 字段"。本次新增 supplier_code 和 supplier_remark 两个字段后，这两个字段将自动出现在接口出参中。需确认：外部报表系统是否能接受新字段（大多数系统忽略未知字段，无影响），还是需要与报表系统对齐契约变化？
>
> 请回答后，我将继续完成集成设计部分分析。

---

**用户回答**：
> 报表系统使用 JSON 解析，忽略未知字段，无需特殊处理。集成设计不需要变更。

---

**🤖 根据用户澄清继续 Step 4（集成设计）**

根据用户澄清（Q5），集成设计**不涉及**。依据：用户确认外部报表系统忽略未知字段，接口契约实质未变化。

**4b. 生成改动点清单**

**CP-001：应用架构**
- source_scenario：UI-01、UI-02（功能特性新增触发 T-01）
- trigger_type：依赖传导（T-01）
- action：修改
- baseline_ref：基线 PRD §2.3 子特性清单
- baseline_state：子特性清单包含"采购单创建"、"采购单查询"、"采购单审批"，无"供应商指定"相关子特性
- target_state：在"采购单管理"特性下扩展"采购单创建/编辑"子特性范围，增加"指定供应商"能力描述（判断：操作对象仍是采购单，无独立业务边界，归入已有子特性而非新增）
- in_scope：§2.3 "采购单创建/编辑"子特性描述
- out_of_scope：特性分类结构不变，不新增子特性节点

**CP-002：界面原型（列表页按钮）**
- source_scenario：UI-01
- trigger_type：主触发
- action：修改
- baseline_ref：基线 PRD §3.1 采购单列表页操作列
- baseline_state：操作列含：查看、编辑、删除
- target_state：操作列新增"指定供应商"按钮，位于"编辑"之后；仅采购员角色可见（依据：用户澄清 Q4）
- in_scope：§3.1 操作列定义
- out_of_scope：列表页其他列、查询条件区域不变

**CP-003：界面原型（弹窗 + Pageflow）**
- source_scenario：UI-02；传导 T-05
- trigger_type：主触发（UI-02）+ 依赖传导（T-05）
- action：新增
- baseline_ref：基线 PRD §3.1 Pageflow 图
- baseline_state：Pageflow 中无"指定供应商"弹窗节点
- target_state：新增"指定供应商"弹窗页面，含字段：供应商编码（必填）、供应商备注（选填）、确认按钮、取消按钮；Pageflow 新增：列表页"指定供应商"按钮 → 弹窗 → 确认保存 → 返回列表页（刷新当前行数据）；取消 → 关闭弹窗不保存
- in_scope：新弹窗页面定义；Pageflow 新增节点
- out_of_scope：列表页其他 Pageflow 路径不变

**CP-004：信息架构**
- source_scenario：DA-01；传导 T-04
- trigger_type：主触发（DA-01）+ 依赖传导（T-04 验证通过，字段不在基线实体中）
- action：修改
- baseline_ref：基线 PRD §4.2 PurchaseOrder 实体字段定义
- baseline_state：PurchaseOrder 字段：order_no、status、amount、creator_id、created_at，无 supplier_code 和 supplier_remark
- target_state：PurchaseOrder 实体新增两个字段：supplier_code（varchar 50，可空，默认 null）、supplier_remark（varchar 200，可空，默认 null）；历史数据处理：默认 null，不补填（依据：用户澄清 Q3）
- in_scope：PurchaseOrder 实体字段定义；数据样例更新
- out_of_scope：其他实体不变；实体关系图不变（无新关联）

**CP-005：功能特性（新增"指定供应商"操作）**
- source_scenario：UI-01、UI-02、DA-01
- trigger_type：主触发
- action：新增
- baseline_ref：基线 PRD §5.x 采购单相关功能特性（无"指定供应商"子特性）
- baseline_state：无"指定供应商"功能特性定义
- target_state：新增功能特性"指定供应商"，包含：UIUX 操作说明（采购员在列表页点击按钮→弹窗录入→保存）；实体操作说明（更新 PurchaseOrder.supplier_code 和 supplier_remark）；业务规则（supplier_code 长度不超过 50 字符；supplier_remark 长度不超过 200 字符；保存成功后刷新列表当前行）；异常处理（保存失败提示"保存失败，请重试"）
- in_scope：新子特性描述范围内的功能
- out_of_scope：采购单其他操作（创建、审批、删除）逻辑不变

**CP-006：权限设计**
- source_scenario：UI-01；用户澄清 Q4
- trigger_type：主触发（需验证 → 确认影响）
- action：修改
- baseline_ref：基线 PRD §6.1 权限矩阵（待完善）
- baseline_state：权限矩阵未定义（章节标注"待完善"）；用户澄清 Q4 明确：仅采购员可见可操作
- target_state：在权限矩阵中，"指定供应商"按钮：采购员=可见可操作；采购主管=不可见；财务=不可见
- in_scope："指定供应商"按钮的角色权限行
- out_of_scope：其他按钮的权限不变

**4c. 生成禁止改动项清单**

**FB-001**
- target：PurchaseOrder.order_no（主键）
- reason：数据库主键，禁止修改
- consequence：主键变更将导致外键引用断裂，影响全系统数据完整性
- adjacent_to：CP-004（信息架构修改涉及同一实体）
- evidence：基线 PRD §4.2 标注 order_no 为主键

**FB-002**
- target：GET /api/purchase-orders 接口签名（路径、入参结构、出参字段语义）
- reason：外部报表系统依赖此接口，现有出参字段语义不可变
- consequence：修改现有字段会导致报表系统解析失败
- adjacent_to：CP-004（新字段会出现在接口出参中，但属于增量，不影响现有字段）
- evidence：基线 PRD §7.1 记录此接口供外部报表系统调用；用户澄清 Q5 确认忽略未知字段但不接受现有字段变化

**FB-003**
- target：采购单列表页（CP-002 in_scope 之外的其他列和查询区域）
- reason：架构边界，本次改动仅限操作列变化
- consequence：修改其他列可能影响其他用户使用中的功能
- adjacent_to：CP-002（操作列修改与其他列物理临近）
- evidence：业务需求原文仅描述"增加供应商指定功能"，无其他列的变更意图

---

**Step 5：拆分增量 Story**

**Story ST-001：采购单列表页新增"指定供应商"按钮**

- 来源：CP-002，场景 UI-01
- 用户故事：作为采购员，我希望在采购单列表页看到"指定供应商"按钮，以便快速进入供应商指定操作。
- 改动范围：
  - 涉及：采购单列表页操作列（新增按钮，仅采购员可见）
  - 不涉及：FB-003（列表页其他列和查询区域）
- 详细说明：在操作列"编辑"按钮之后新增"指定供应商"按钮；按钮仅对角色为采购员的用户显示；其他角色的操作列不显示此按钮。
- 验收标准：
  - AC-001（功能验证）
    - Given：采购单列表页已加载，当前登录用户角色为采购员
    - When：查看任意采购单行的操作列
    - Then：操作列显示"查看"、"编辑"、"指定供应商"、"删除"四个按钮，顺序如上
  - AC-002（权限验证）
    - Given：采购单列表页已加载，当前登录用户角色为采购主管
    - When：查看任意采购单行的操作列
    - Then：操作列显示"查看"、"编辑"、"删除"，不显示"指定供应商"按钮；DOM 中该行内不存在文本为"指定供应商"的按钮元素
- 关联 PRD 章节：界面原型 §3.1、权限设计 §6.1

**Story ST-002：新增"指定供应商"录入弹窗**

- 来源：CP-003，场景 UI-02
- 用户故事：作为采购员，我希望点击"指定供应商"按钮后弹出录入窗口，以便填写并保存供应商信息。
- 改动范围：
  - 涉及：新弹窗页面定义（字段、按钮）；Pageflow 新增弹窗路径
  - 不涉及：FB-003（列表页其他 Pageflow 路径）
- 详细说明：弹窗包含供应商编码（必填，varchar 50）、供应商备注（选填，varchar 200）、确认按钮、取消按钮；确认后保存并刷新列表当前行；取消后关闭弹窗不保存。
- 验收标准：
  - AC-003（功能验证）
    - Given：采购单列表页，采购员点击某行"指定供应商"按钮
    - When：弹窗打开
    - Then：弹窗显示"供应商编码"输入框（标注必填）、"供应商备注"文本域（无必填标注）、"确认"按钮、"取消"按钮
  - AC-004（边界验证 - Negative）
    - Given：弹窗已打开，供应商编码输入框为空
    - When：点击"确认"按钮
    - Then：表单不提交，供应商编码输入框下方显示必填提示文字，弹窗保持打开
  - AC-005（边界验证 - Negative）
    - Given：弹窗已打开，供应商编码输入超过 50 个字符
    - When：点击"确认"按钮
    - Then：表单不提交，供应商编码输入框下方显示长度限制提示，弹窗保持打开
- 关联 PRD 章节：界面原型 §3.1（Pageflow）

**Story ST-003：PurchaseOrder 实体新增供应商字段**

- 来源：CP-004，场景 DA-01
- 用户故事：作为系统，我需要将采购员指定的供应商信息持久化到采购单记录中，以便后续查看和使用。
- 改动范围：
  - 涉及：PurchaseOrder 实体新增 supplier_code、supplier_remark 字段；数据样例更新
  - 不涉及：FB-001（PurchaseOrder.order_no 主键不变）；FB-002（接口出参现有字段语义不变）
- 详细说明：PurchaseOrder 实体新增 supplier_code（varchar 50，可空，默认 null）、supplier_remark（varchar 200，可空，默认 null）；历史数据：存量采购单两个字段默认 null，不执行补填迁移。
- 验收标准：
  - AC-006（数据验证）
    - Given：弹窗中输入 supplier_code = "SUP-001"，supplier_remark 留空，点击确认保存成功
    - When：查询数据库
    - Then：对应 purchase_order 记录中 supplier_code = "SUP-001"，supplier_remark = null
  - AC-007（数据验证 - 历史数据）
    - Given：系统升级部署后，查询升级前已存在的任意采购单记录
    - When：查询 purchase_order 表
    - Then：该记录 supplier_code = null，supplier_remark = null，记录其余字段值与升级前一致，无数据丢失
- 关联 PRD 章节：信息架构 §4.2

**Story ST-004：指定供应商功能特性定义**

- 来源：CP-001（应用架构）、CP-005（功能特性）
- 用户故事：作为采购员，我希望能完整执行供应商指定操作，系统按照业务规则保存和校验数据。
- 改动范围：
  - 涉及：功能特性"指定供应商"完整描述；应用架构 §2.3 子特性范围扩展
  - 不涉及：采购单创建、审批、删除等其他功能特性逻辑
- 详细说明：功能逻辑：采购员在列表页点击"指定供应商"→弹窗打开→填写信息→点确认→系统校验（supplier_code 非空且≤50字符，supplier_remark ≤200字符）→校验通过→保存到 PurchaseOrder →返回列表页刷新当前行。
- 验收标准：
  - AC-008（功能验证 - 正常流程）
    - Given：采购单列表页，采购员对采购单 PO-001（当前 supplier_code = null）点击"指定供应商"
    - When：弹窗中输入 supplier_code = "SUP-001"，点击确认
    - Then：弹窗关闭，列表页 PO-001 行数据刷新（不整页刷新），PO-001 的 supplier_code 持久化为"SUP-001"（可通过 ST-003 的 AC-006 验证）
  - AC-009（功能验证 - 异常流程）
    - Given：数据库保存操作失败（如网络超时）
    - When：采购员点击确认
    - Then：弹窗不关闭，页面显示提示"保存失败，请重试"，采购单数据未发生变化
- 关联 PRD 章节：应用架构 §2.3、功能特性 §5.x

---

**🤖 Step 1-5 完成，输出草案**

```
=== 增量 PRD 影响域分析草案 ===

【一、原子变化场景】
UI-01、UI-02、DA-01、DA-07（共4个）

【二、受影响 PRD 要素总表】
应用架构    | 依赖传导 T-01      | 修改 | 扩展子特性描述
界面原型    | 主触发+传导 T-05   | 修改/新增 | 新增按钮、弹窗、Pageflow
信息架构    | 主触发+传导 T-04   | 修改 | 新增两个字段
功能特性    | 主触发            | 新增 | 新增指定供应商功能特性
权限设计    | 主触发（验证确认）  | 修改 | 按角色控制按钮可见性

【三、不涉及要素说明】
场景解决方案 | 单步操作不改变端到端链路 | 基线无相关场景，操作为独立弹窗
集成设计    | 用户澄清Q5确认无影响 | 接口忽略新字段，现有字段语义不变
配置设计    | 无参数化需求 | 业务需求中无配置项描述
非功能需求  | 无新指标约束 | 业务需求中无性能/安全/合规要求

【四、改动点明细】
CP-001 应用架构修改、CP-002 列表按钮、CP-003 弹窗+Pageflow、
CP-004 实体字段、CP-005 功能特性新增、CP-006 权限矩阵

【五、禁止改动项清单】
FB-001 PurchaseOrder.order_no 主键禁止修改
FB-002 GET /api/purchase-orders 现有字段语义禁止变化
FB-003 列表页其他列和查询区域禁止改动

【六、增量 Story】
ST-001 列表页按钮（含权限验证）
ST-002 录入弹窗+Pageflow
ST-003 实体字段新增+历史数据
ST-004 功能特性定义+应用架构子特性扩展

=== 待确认问题汇总 ===
无（所有低置信度问题均已通过澄清解决）

---
请确认以上分析结论，有无遗漏或错误？确认后我将生成增量 PRD 文档。
```

---

### 案例 B：基线 PRD 完整，需求清晰，无需澄清

**场景背景**：

> **业务需求**：采购单列表，在现有列之后加一列"创建人部门"展示。

**基线 PRD 摘录**：
> - §3.1 采购单列表页：列定义包含 order_no、status、amount、creator_id、created_at
> - §4.2 PurchaseOrder 实体：字段包含 creator_dept（varchar 100，可空），**已存在**
> - §5.x 功能特性"采购单查询"：列表展示字段清单未包含 creator_dept

**🤖 Skill 执行（无暂停，全程有据可查）**

**Step 1 识别原子场景**

- "加一列"→ UI-03（已有页面新增字段展示）
- evidence：原文"采购单列表，在现有列之后加一列`创建人部门`展示"
- confidence：高

**Step 2 推导主触发要素**

UI-03 → 必然影响：界面原型、功能特性；需验证：信息架构

**Step 3 依赖传导检查**

- 功能特性预期为"修改"（不是新增） → T-01 不触发
- 无新增页面 → T-04、T-05 不触发
- 无删除页面 → T-06 不触发
- 所有传导规则均不触发

**Step 4 改动点分析**

**信息架构（需验证）**：查基线 §4.2，PurchaseOrder 实体已存在 creator_dept 字段（varchar 100，可空）。**信息架构不涉及**。依据：基线 PRD §4.2 明确记录该字段已存在。

**界面原型**：

CP-001：界面原型，修改
- baseline_ref：基线 PRD §3.1 采购单列表列定义
- baseline_state：列清单为 order_no、status、amount、creator_id、created_at，共5列
- target_state：在 created_at 列之后新增"创建人部门"列，展示 PurchaseOrder.creator_dept 值；creator_dept 为空时显示"—"
- in_scope：§3.1 列定义
- out_of_scope：其他列顺序和定义不变；查询条件不变

**功能特性**：

CP-002：功能特性，修改
- baseline_ref：基线 PRD §5.x 功能特性"采购单查询"列表展示字段清单
- baseline_state：列表展示字段清单未包含 creator_dept
- target_state：列表展示字段清单新增 creator_dept，对应界面列"创建人部门"；列表查询接口出参需包含 creator_dept 字段
- in_scope：§5.x 列表展示字段说明、实体操作说明
- out_of_scope：其他功能特性不变

**禁止改动项**：

FB-001：采购单列表其他列定义禁止变更。原因：业务需求仅说明"加一列"，其他列无变更意图。依据：业务需求原文。

**Step 5 Story**

**Story ST-001：采购单列表新增"创建人部门"列**

- 来源：CP-001、CP-002，场景 UI-03
- 用户故事：作为采购员，我希望在采购单列表中看到每条记录的创建人部门，以便了解采购单的来源部门。
- 改动范围：
  - 涉及：列表页列定义新增"创建人部门"；功能特性列表展示字段更新
  - 不涉及：FB-001（其他列定义不变）；信息架构不变（字段已存在）
- 详细说明：在 created_at 列之后新增"创建人部门"列，取值来源 PurchaseOrder.creator_dept；当 creator_dept 为空时，列显示"—"而非空白。
- 验收标准：
  - AC-001（功能验证）
    - Given：采购单列表已加载
    - When：查看列表表头
    - Then：表头最后一列为"创建人部门"，位于"创建时间"之后
  - AC-002（数据验证）
    - Given：数据库中某采购单记录 creator_dept = "采购一部"
    - When：该记录在列表中展示
    - Then：该行"创建人部门"列显示"采购一部"
  - AC-003（边界验证）
    - Given：数据库中某采购单记录 creator_dept = null
    - When：该记录在列表中展示
    - Then：该行"创建人部门"列显示"—"（短横线），不显示空白
- 关联 PRD 章节：界面原型 §3.1、功能特性 §5.x

**🤖 输出草案**（格式同案例 A，此处省略完整格式）：无待确认问题，5个步骤全部完成，无暂停触发，直接输出草案供用户确认。

---

*案例 B 说明了"有理有据"在无歧义场景下的表现：每条结论都引用了基线 PRD 章节，信息架构"不涉及"的结论有明确依据（字段已存在），不需要猜测，也不需要澄清。*
