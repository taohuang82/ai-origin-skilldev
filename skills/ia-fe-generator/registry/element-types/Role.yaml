element_type: Role
name_cn: 角色
schema_version: "1.0.0"
common_fields:
  required: [id, element_type, name, status]
  optional: [domain, provenance, lineage, code_anchors, external_refs]
fields:
  - { name: role_category, type: enum, required: true, enum: [用户角色, 系统角色, 外部系统角色], desc: 角色类型 }
  - { name: responsibility_boundary, type: string, required: true, desc: 职责边界 }
  - { name: dynamic_assignment, type: bool, required: false, desc: 是否动态分配 }
  - { name: delegation_supported, type: bool, required: false, desc: 是否支持代理/委托 }
relations_held: []
stage_increments: {}
