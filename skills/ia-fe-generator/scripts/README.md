# scripts/ — 部署期脚本（spec → 标准 agent 抽取）

本目录预留 spec→agent 抽取脚本（《构建规范 v1.5》第七章）。部署期把
（engine/element-runner.md 模板 + spec/{element}.md + 引用 standards）
预烘焙成标准 agent 文件，copy 进项目 .claude/agents/。本期（0→1 构建）仅预留接口，不实现。
