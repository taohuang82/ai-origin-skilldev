---
module_id: "m-fe-business-function"
implements: "business-function"
for_scenario: ["专题需求", "优化需求"]
for_type: ["TP"]  # TP类型专用要素
execution_mode: ["build", "modify", "incremental"]
status: "active"
---

# m-fe-business-function — 业务功能分析(TP专用)

> 从业务活动提取功能清单,汇聚活动明细生成功能详细描述,设计权限矩阵表格(角色×功能)。回溯业务流程活动总览表格,为每个功能分配编号,标注优先级。

## 目标

**目标说明**

从业务活动总览表格提取活动,为每个活动分配功能编号,汇聚活动明细生成功能详细描述,设计角色×功能的权限矩阵表格。

**输出物**

- 功能清单表格(功能编号、功能名称、功能说明、对应活动、优先级)
- 功能详细描述表格(功能编号、功能名称、所属模块、核心功能点、操作步骤、涉及实体、业务规则)
- 权限矩阵表格(角色×功能的权限矩阵,标注查看/编辑/删除/审批等权限类型)

**成功标准**

- 功能编号全局唯一(格式FR-子系统-模块-序号)
- 功能清单覆盖所有业务活动
- 功能描述包含核心功能点、操作步骤、涉及实体、业务规则
- 权限矩阵完整性(覆盖所有角色和功能)
- 权限矩阵用户确认

---

## 前置条件

**依赖要素**

| 依赖要素 element_id | 原因 |
|----------------------|------|
| business-process | 业务流程活动总览表格已生成,角色清单已生成 |
| user-interaction(构思) | 功能描述需要引用页面信息(但user-interaction在后续执行,此处仅构思) |

**必要输入**

- 业务流程-活动总览表格
- 业务流程-活动明细
- 业务流程-角色清单

---

## 约束

### 格式规范

| standard_id | 规范说明 |
|-------------|---------|
| (暂无) | 本要素无 Mermaid 图等格式输出 |

### 设计约束

| 约束编号 | 级别 | 规则 | 验证方法 |
|----------|------|------|---------|
| `DC-FUNC-001` | MUST | 功能编号必须全局唯一,格式为FR-子系统-模块-序号 | 检查功能清单是否存在重复编号 |
| `DC-FUNC-002` | MUST | 功能编号必须标注对应活动编号(ACT-xxx) | 检查功能清单是否包含对应活动列 |
| `DC-FUNC-003` | MUST | 功能描述必须包含核心功能点、操作步骤(5-7步)、涉及实体、业务规则编号(BR-xxx) | 检查功能详细描述表格字段完整性 |
| `DC-FUNC-004` | MUST | 权限矩阵必须覆盖所有角色和功能,标注权限类型(查看/编辑/删除/审批等) | 检查权限矩阵表格完整性 |
| `DC-FUNC-005` | MUST | 权限矩阵必须获得用户交互确认 | 检查对话记录是否包含用户确认环节 |

---

## 执行步骤

**Step 1: 从业务活动总览表格提取活动** `[自动]`

读取 `business-process` 要素生成的活动总览表格:
- 每个活动对应一个功能(ACT-xxx → FR-xxx)
- 若一个活动包含多个子操作,可拆分为多个功能

**Step 2: 分配功能编号** `[自动]`

为每个功能分配全局唯一编号:
- 格式: `FR-{子系统}-{模块}-{序号}`
- 示例: `FR-Finance-Reimbursement-001` (财务-报销管理-001)

**若子系统/模块信息未知**:
- 暂用通用编号: `FR-System-Main-001`
- 后续用户交互补充子系统/模块信息后更新

**Step 3: 形成功能清单表格** `[自动]`

| 功能编号 | 功能名称 | 功能说明 | 对应活动 | 优先级 |
|---------|---------|---------|---------|--------|
| FR-xxx | {功能名称} | {功能说明} | ACT-xxx | P0/P1/P2 |

标注优先级:
- P0: 核心功能,必须实现
- P1: 重要功能,首期实现
- P2: 辅助功能,可延后

**用户交互确认功能清单**:

> "我已经从业务流程中提取了以下功能清单,请确认是否准确:
>
> {展示功能清单表格}
>
> 这个功能划分是否合理?优先级是否合适?"

用户确认后,进入功能详细描述。

---

**Step 4: 汇聚活动明细生成功能详细描述** `[自动]`

从 `business-process` 要素的活动明细汇聚:

| 字段 | 来源 |
|------|------|
| 功能编号 | 功能清单 |
| 功能名称 | 活动名称 |
| 所属模块 | 功能编号中的模块部分 |
| 核心功能点 | 活动明细的"操作内容"字段精华提取 |
| 操作步骤 | 活动明细的"操作内容"拆解为5-7步,标注对应页面和触发按钮(构思) |
| 涉及实体 | 活动明细的"输入信息"和"输出结果"涉及的实体(引用后续信息架构章节实体编号) |
| 业务规则 | 活动明细的"业务规则"字段引用(规则编号BR-xxx) |

生成功能详细描述表格:

| 功能编号 | 功能名称 | 所属模块 | 核心功能点 | 操作步骤 | 涉及实体 | 业务规则 |
|---------|---------|---------|-----------|---------|---------|---------|
| FR-xxx | {功能名称} | {模块} | {功能点} | {步骤1-7} | {实体编号} | BR-xxx |

**补充操作步骤** `[交互]`

若活动明细的操作内容不够细化:
- 追问:"这个功能的具体操作步骤是什么?比如第1步用户做什么,第2步做什么?"
- 拆解为5-7步,标注对应页面(构思):
  - "第1步:用户在'{页面名称}'页面点击'{按钮名称}'按钮"
  - "第2步:系统弹出表单,用户填写'{字段名称}'字段"
  - ...

**补充涉及实体** `[交互]`

若活动明细未明确涉及实体:
- 追问:"这个功能涉及哪些核心业务实体?(如订单、申请单、审批记录)"
- 记录实体名称(后续信息架构章节生成实体编号后引用)

---

**Step 5: 设计权限矩阵表格** `[自动]`

基于角色清单和功能清单设计权限矩阵:

**权限矩阵表格格式**:

| 角色 | FR-001(功能1) | FR-002(功能2) | FR-003(功能3) | ... |
|------|--------------|--------------|--------------|-----|
| 角色1 | 查看、编辑 | 查看 | 编辑、审批 | ... |
| 角色2 | 查看 | 查看、编辑、删除 | 查看 | ... |
| 角色3 | 编辑、审批 | 查看 | 查看、编辑 | ... |

**权限类型定义**:
- 查看: 可查看功能对应页面内容
- 编辑: 可修改功能对应数据
- 删除: 可删除功能对应记录
- 审批: 可执行审批操作(通过/驳回)
- 导出: 可导出功能对应数据
- 打印: 可打印功能对应内容

---

**Step 6: 交互确认权限矩阵** `[交互]` ⚠️ **强制环节**

> "我已经基于角色清单和功能清单设计了权限矩阵,请确认每个角色的权限范围是否准确:
>
> {展示权限矩阵表格}
>
> **逐角色追问**:
> - 角色1的权限: 查看、编辑功能1,查看功能2... 这个权限范围是否合适?
> - 角色2的权限: 查看、编辑、删除功能2,查看功能3... 这个权限范围是否合适?
> - ..."

**逐角色确认**:
- 若用户提出修正: 更新权限矩阵
- 若用户说某个角色权限设置不合理: 追问"应该改成什么权限?"

**最终确认**:

> "权限矩阵已确认,这个权限分配是否合理?"

强制获得明确确认。

---

**Step 7: 写入文档** `[自动]`

### incremental 模式

> 适用于增量构建场景。基线为 context.base_doc_path 指向的历史 FE。
> 本节仅生成"变更增量"(用 DELTA 标注块包裹),不重写已有内容。

**Step I-1: 读取基线对应章节** `[自动]`

读取 `context.base_doc_path` 中本要素对应章节(依据 `chapter_info.l1_no`)的现有内容,作为基线。
若基线对应章节不存在(异常情况) → 暂停并报错,提示用户基线 FE 不完整。

**Step I-2: 提取本要素相关的变化点** `[自动]`

从 `context.impact_analysis.element_changes` 提取本要素相关的变化点列表,每项含:
- `change_id`(如 PR-01)
- `user_description`(用户原话)
- `impact_level`(certain/likely/conditional/always_affected)

若 `element_changes` 为空 → 跳过本要素(在 Phase 6 仅记录 SKIP)。

聚焦:针对 FN-* 和 PR-04 变化点。FN-01 追问新功能描述+对应活动;FN-02 追问操作步骤变化;FN-03 追问下线功能的依赖;FN-04 追问权限矩阵变更

**Step I-3: 对话挖掘变化细节** `[交互]`

对每个变化点,沿用本 Spec `## 追问维度` 的提问内核,但**只针对变化部分追问**,禁止重新挖掘基线已有内容。

具体追问聚焦点见下方"逐 Spec 微调点"。

每完成一个变化点的细节挖掘,做小结并请用户确认,确认后才继续下一个变化点。

**Step I-4: 生成 DELTA 标注的增量内容** `[自动]`

按 element-runner Phase 4 incremental 分支的 DELTA 格式输出:

```
<!-- DELTA: change={change_id}, chapter={element_id}, op={add|modify|delete}, level={certain|likely|conditional} -->
{增量内容,保留 Markdown 格式,含表格/Mermaid/列表}
<!-- /DELTA -->
```

注意:
- 仅输出增量部分,不复述基线已有内容
- 多个变化点对应多个 DELTA 块
- `op=modify` 必须明确"修改自基线哪一节"(在 DELTA 块内首行写"基线引用: {chapter}")
- `op=delete` 必须明确"删除基线哪一节"(在 DELTA 块内写"删除目标: {chapter}, 原因: {...}")

**Step I-5: 累积 ImpactPoint** `[自动]`

将本要素本次产生的影响点写入 `context.impact_analysis.impact_points`,数据结构遵循规范 §3.7.3:

```yaml
- id: "IP-{全局递增}"
  source_change: "{change_id}"
  trigger_type: "primary"
  element: "{当前 element_id}"
  kind: "modify"
  baseline_ref: "{基线章节引用}"
  baseline_state: "{基线现状描述}"
  action: "新增 | 修改 | 删除"
  target_state: "{目标状态}"
  in_scope: ["{明确包含的对象}"]
  out_of_scope: ["{明确排除的对象}"]
```

**Step I-6: 用户最终确认** `[交互]`

展示本要素本次的全部 DELTA 块和 ImpactPoint 清单,询问"本要素的增量是否准确?"。

强制获得明确确认("准确"/"没问题"/"对的")后才能进入 Phase 5 质量验证。

---

## 引导开场(对话模式)

我已经从业务流程中提取了功能清单和权限矩阵,请确认是否准确。

---

## 追问维度

### 功能清单

- 功能划分: 每个功能是否合理拆分?
- 功能名称: 功能名称是否准确?
- 功能说明: 功能说明是否清晰?
- 优先级: P0/P1/P2划分是否合理?

### 功能详细描述

- 核心功能点: 功能的核心价值是什么?
- 操作步骤: 用户如何操作这个功能?(5-7步)
- 涉及实体: 涉及哪些核心业务实体?
- 业务规则: 引用业务规则编号(BR-xxx)

### 权限矩阵

- 角色权限范围: 每个角色对每个功能的权限(查看/编辑/删除/审批)是否合理?
- 权限完整性: 是否覆盖所有角色和功能?

---

## 完整性检查

检查必要信息是否完整:
- [ ] 功能清单已生成(包含功能编号、功能名称、功能说明、对应活动、优先级)
- [ ] 功能编号全局唯一(格式FR-子系统-模块-序号)
- [ ] 功能详细描述已生成(包含核心功能点、操作步骤、涉及实体、业务规则)
- [ ] 权限矩阵已生成(覆盖所有角色和功能)
- [ ] 权限矩阵已获得用户明确确认

---

## 用户确认

展示功能清单和权限矩阵,询问:"这个功能划分和权限分配是否合理?"

强制获得明确确认。

**特别要求**:
- 权限矩阵必须逐角色追问并获得确认
- 功能编号必须标注对应活动编号

---

## 强制质量检查

- ✅ 功能编号全局唯一,格式正确
- ✅ 功能清单覆盖所有业务活动
- ✅ 功能详细描述包含核心功能点、操作步骤、涉及实体、业务规则
- ✅ 权限矩阵覆盖所有角色和功能
- ✅ 权限矩阵已获得用户确认
- ✅ 无占位符

---

## 输出骨架

```markdown
## 五、业务功能

### 功能清单

| 功能编号 | 功能名称 | 功能说明 | 对应活动 | 优先级 |
|---------|---------|---------|---------|--------|
| FR-xxx | {功能名称} | {功能说明} | ACT-xxx | P0/P1/P2 |

### 功能详细描述

| 功能编号 | 功能名称 | 所属模块 | 核心功能点 | 操作步骤 | 涉及实体 | 业务规则 |
|---------|---------|---------|-----------|---------|---------|---------|
| FR-xxx | {功能名称} | {模块} | {功能点} | {步骤1-7} | {实体编号} | BR-xxx |

**FR-xxx: {功能名称}详细说明**

- **核心功能点**: {功能点描述}
- **操作步骤**:
  1. 用户在"{页面名称}"页面点击"{按钮名称}"按钮
  2. 系统弹出表单,用户填写"{字段名称}"字段
  3. ...
- **涉及实体**: {实体名称}(实体编号:Entity-xxx)
- **业务规则**: {规则编号BR-xxx列表}

(重复每个功能)

### 业务权限矩阵

| 角色 | FR-001(功能1) | FR-002(功能2) | FR-003(功能3) | ... |
|------|--------------|--------------|--------------|-----|
| 角色1 | 查看、编辑 | 查看 | 编辑、审批 | ... |
| 角色2 | 查看 | 查看、编辑、删除 | 查看 | ... |
| 角色3 | 编辑、审批 | 查看 | 查看、编辑 | ... |

> 注:本权限矩阵基于角色清单和功能清单设计,已获得用户逐角色确认。权限类型包括:查看、编辑、删除、审批、导出、打印。

> ⚠️ **特别说明**: 功能编号标注对应活动编号(ACT-xxx),便于追溯业务流程。功能描述的涉及实体和业务规则引用后续章节实体编号和规则编号。
```