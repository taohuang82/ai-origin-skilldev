---
module_id: "m-fe-{要素英文ID}"
implements: "{要素英文ID}"
for_scenario: ["专题需求", "优化需求"]
for_type: ["TP", "AP", "AI"]  # 根据要素所属类型调整,若TP专用则只填["TP"]
execution_mode: ["build", "modify", "incremental"]
status: "active"  # 或 "planned" 若待实现
dual_input_mode: false  # 仅 original-requirement 为 true,其他要素均为 false
---

# m-fe-{要素英文ID} — {要素中文名称}

> {要素用途描述,一句话概括要素目标}

## 目标

**目标说明**

{目标说明内容,描述本要素要解决什么问题、挖掘什么信息}

**输出物**

- {输出物清单,列出本要素输出的表格/图表/文档}

**成功标准**

- {成功标准清单,列出本要素完成的判据}

---

## 前置条件

**依赖要素**

| 依赖要素 element_id | 原因 |
|----------------------|------|
| {依赖要素ID或"(无依赖)"} | {依赖原因} |

**必要输入**

- {必要输入描述,列出本要素执行前需要准备的材料}

---

## 约束

### 格式规范

| standard_id | 规范说明 |
|-------------|---------|
| {规范ID或"(暂无)"} | {规范说明,若无外部规范则填"(暂无)"} |

### 设计约束

| 约束编号 | 级别 | 规则 | 验证方法 |
|----------|------|------|---------|
| {约束编号,如DC-ELEMENT-001} | MUST | {约束规则} | {验证方法} |

---

## 执行步骤

**Step 1: {步骤名称}** `[自动]` 或 `[交互]`

{步骤描述}

**Step 2: {步骤名称}** `[自动]` 或 `[交互]`

{步骤描述}

...

**Step N: 写入文档** `[自动]`

---

## 引导开场(对话模式)

{引导开场的具体话术,用于对话式发现开场引导用户}

---

## 追问维度

{追问维度清单,列出本要素需要追问的具体问题维度}

---

## 完整性检查

检查必要信息是否完整:
- [ ] {完整性检查项1}
- [ ] {完整性检查项2}
- ...

---

## 用户确认

{用户确认的具体流程,描述如何展示整理结果并获得用户确认}

---

## 强制质量检查

- ✅ {质量检查项1}
- ✅ {质量检查项2}
- ...

---

## 输出骨架

```markdown
{输出内容的markdown骨架,展示本要素写入FE文档的章节结构}

示例:
## {要素中文名称}

{表格/图表格式}
```

---

## 备注(可选)

{补充说明,如本要素的特殊处理逻辑、与其他要素的差异等}