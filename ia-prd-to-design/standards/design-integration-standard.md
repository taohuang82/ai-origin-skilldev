---
standard_id: "design-integration"
name: "集成与异步（合并）"
version: "1.0.0"
---

# design-integration

内建规范；域 **`integration`**；子分段：`integration-mq`, `integration-external`, `integration-notification`。
每节含「规范条文」与「交付模板」（若该分段定义了模板章节）。

## 分段：`integration-mq`


**规范分段**：`integration-mq`

### 规范条文

# MQ 清单设计规范（TDD 第5章 §5.1）

## 适用场景

以下情形**必须**设计 MQ 要素：
- 业务流程需要异步解耦（如订单创建后异步发送通知）
- 耗时操作需要削峰（如大批量数据导出）
- 跨微服务事件驱动（如状态变更触发下游联动）

## Topic 命名规范

格式：`T_{业务域}_{动作}` 如: 

示例：
- `T_KRI_MODEL_DATASET_CHANGE` — KRI模型数据集变更消息
- `T_PROJECT_ACTIVITY_CREATE` — 项目创建消息

### 环境隔离策略

**测试环境共用MQ服务器,通过Topic名称区分:**
- dev环境: `T_KRI_MODEL_DATASET_CHANGE_DEV`
- sit环境: `T_KRI_MODEL_DATASET_CHANGE_SIT`
- uat环境: `T_KRI_MODEL_DATASET_CHANGE_UAT`
- 生产环境: `T_KRI_MODEL_DATASET_CHANGE`

## 消息体 Schema 规范

- 使用 JSON 格式，字段名驼峰命名
- 必须包含：`businessId`（业务唯一ID）、`messageId` (幂等键)、业务字段
- 可选：`need_order` 是否需要顺序消费、`need_reply` 是否需要回执
- 禁止在消息体中传递大对象（如完整的订单详情），传 ID 让消费者自行查询

## 可靠性设计

### 发送端（Producer）

- 关键消息使用**事务消息**或**本地消息表**保证不丢失
- 发送失败需要重试，重试次数有上限（通常 3 次），超出记录数据库异步补偿
- 发送 MQ 必须在**事务提交后**执行（禁止在事务内发 MQ）

### 消费端（Consumer）

- 必须实现**幂等消费**（幂等键 = `messageId`）
- 消费失败的错误处理：
  - 可重试错误（如网络超时）：重试 N 次（指数退避）
  - 不可重试错误（如数据格式错误）：直接进死信队列（DLQ），人工处理
- 消息乱序：设置orderGroupId消息分组ID，MsgGroupId相同的消息在消费时能保证有序

### 积压告警

- 消费者组积压监控，超过阈值触发告警
- 消费者实例数可按积压量弹性扩容

## 消息顺序

| 场景 | 方案 |
|------|------|
| 顺序消息（同一订单的事件有序） | 使用 orderId 作为分区键（shardingKey） |
| 无顺序要求 | 不设 shardingKey，提升并发 |

---

## 项目规范摘录（`规范/` 目录 · IT 设计文档 §4.5）

> 来源：`{WORKSPACE_ROOT}/规范/examples/it_design_doc.md`「MQ设计」章节。

- **MQ 设计场景**：每条需求须标明 **新增 / 修改 / 删除** 之一。
- **Topic 名称**：与本文档 Topic 命名规范一致，并在设计说明中写清业务含义。
- **消息结构体**：用 **标准 JSON** 给出字段定义（可与上文消息体 Schema 合并输出）。

### 交付模板

# MQ 清单设计（TDD 第5章 §5.1）

<!--
变更标注约定：
- ✨ 新增：本次新增的 Topic/Tag
- 🔧 修改：Schema / 消费者组 / 策略变更
-->

## 变更概要

| Topic | Tag | MQ 场景（新增/修改/删除） | 动作 | 说明 |
|-------|-----|--------------------------|------|------|
| | | 新增 | ✨/🔧 | |

---

## MQ 清单

| Topic          | Tag | 发布者 | 消费者 | 顺序消息 | 动作 |
|----------------|-----|--------|--------|---------|------|
| `T_{业务域}-{动作}` | 可为空 | `{service}-producer` | `{service}-consumer` | Y/N | ✨/🔧 |

---

## ✨/🔧 Topic：{topic-name} / Tag：{TAG_NAME}

> 🔧 **变更说明**：{仅修改时填写，Schema 变更需说明字段增减及兼容策略}

### 基本信息

| 属性 | 值                         |
|------|---------------------------|
| Topic | `T_{业务域}-{动作}`            |
| Tag | `{RESOURCE_ACTION}`       |
| 发布者组 | `{service-name}-producer` |
| 订阅者组 | `{service-name}-consumer` |
| 消息顺序 | 有序（消息分组Id：{orderGroupId}）/ 无序  |

### 消息体 Schema

```json
{
  "eventId": "string（UUID，幂等键）",
  "eventType": "RESOURCE_ACTION",
  "occurredAt": "timestamp（ISO 8601）",
  "{businessId}": "string（业务主键）",
  "{otherField}": "..."
}
```

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `eventId` | String | Y | 幂等键，全局唯一 |
| `eventType` | String | Y | 事件类型枚举 |
| `occurredAt` | String | Y | 事件发生时间，ISO 8601 |
| `{businessId}` | String | Y | 业务主键 |

### 触发源

| 触发操作 | 触发方法 | 说明 |
|---------|---------|------|
| {业务操作} | `{ServiceImpl}.{method}()` | {发生在事务提交后} |

### 消费方与处理逻辑

| 消费者服务 | 消费者组 | 处理逻辑摘要 | 幂等键 |
|----------|---------|------------|--------|
| | `{service}-consumer` | | `eventId` |

### 可靠性设计

| 场景 | 方案 |
|------|------|
| 发送保障 | 事务消息 / 本地消息表（`{table_name}`） |
| 消费幂等 | 幂等键 = `eventId`，Redis SETNX 去重 |
| 消费失败 | 重试 3 次（指数退避），超限进 DLQ |
| 积压告警 | 消费者组积压 > {N} 条触发告警 |

## 分段：`integration-external`


**规范分段**：`integration-external`

### 规范条文

# 外部服务依赖规范（TDD 第5章 §5.2）

## 适用场景

本要素记录本系统**跨业务边界调用外部系统**的依赖契约，包括：
- 调用其他微服务（FeignClient / HTTP）
- 调用第三方平台（支付、短信、地图、OCR 等）
- 调用组织架构、用户中心等**独立部署的基础业务服务**

## 不属于本要素

以下能力由 **Jalor / IAOne 平台框架或二方件**提供，属于框架内置用法或平台 SDK 接入，**不得**作为 §5.2 外部服务依赖输出：

| 能力 | 应归属要素 |
|------|-----------|
| Jalor 权限注解（`@JalorResource` / `@JalorOperation`） | `config-permission` |
| Jalor Lookup / 数据字典查询 | `config-dict` / `config-app-config` |
| Jalor 异步任务、框架内置 MQ 封装 | `backend/be-schedule` 或 `architecture/arch-tech-stack` |
| Jalor Excel 导入 / 导出 | `backend/be-export` |
| Jalor 业务异常与错误码 | `config-error-code` |
| IAOne 审批流 SDK | `backend/be-workflow` |
| Todos / Espace / EDM 等平台 SDK | 对应 `backend` 实现要素（按业务场景） |

> 判断原则：若依赖的是**平台框架 API / SDK 用法**而非**独立外部系统的 HTTP/RPC 契约**，则不属于集成域。

## 依赖声明原则

- 每个外部依赖必须声明超时（connectTimeout + readTimeout）
- 必须有熔断器（Resilience4j / Sentinel）保护
- 必须有降级返回值（不允许返回 null 后直接使用）
- 同步调用不能超过 3 跳（避免调用链过深）

## 超时配置规范

| 场景 | connectTimeout 建议 | readTimeout 建议 |
|------|-------------------|----------------|
| 内部微服务 | 500ms | 2000ms |
| 组织架构等基础服务 | 500ms | 3000ms |
| 第三方平台（支付/短信） | 1000ms | 5000ms |
| 数据密集型接口 | 500ms | 10000ms |

## 熔断器配置规范

- 失败率阈值：50%（超过此比例触发熔断）
- 慢调用阈值：P95 > 2s 视为慢调用
- 熔断持续时间：30s（进入半开状态后探测恢复）
- 最小调用数：10 次（样本量不足时不触发熔断）

## 降级策略规范

| 降级类型 | 适用场景 | 示例 |
|---------|---------|------|
| 返回默认值 | 查询类，结果不影响主流程 | 获取用户头像失败返回默认头像 |
| 返回缓存值 | 数据变化不频繁 | 组织架构信息返回本地缓存 |
| 跳过操作 | 非核心操作 | 发送通知失败不影响主业务 |
| 抛出业务异常 | 核心操作不可降级 | 支付结果查询失败必须终止 |

## 重试策略规范

- 重试仅适用于网络抖动等**可重试错误**（超时、5xx）
- 业务错误（4xx、参数错误）不重试
- 最多重试 2 次，使用指数退避（1s、2s）
- 重试时注意接口幂等性（GET / 幂等 POST 可重试）

---

## 项目规范摘录（`规范/` 目录 · IT 设计文档 §4.6）

> 来源：`{WORKSPACE_ROOT}/规范/examples/it_design_doc.md`「对现有业务影响评估」中与**接口/外部调用**相关的条目。

- 列出**受影响接口**及对测试的影响类型、描述、测试重点，便于界定联调与回归范围。
- 若变更涉及对外契约或第三方集成，须与本文档「超时 / 熔断 / 降级 / 重试」章节一并评估兼容性与爆炸半径。

### 交付模板

# 外部服务依赖设计（TDD 第5章 §5.2）

<!--
变更标注约定：
- ✨ 新增：本次新增的外部依赖
- 🔧 修改：超时/重试/降级策略变更
-->

## 变更概要

| 外部服务 | 接口类型 | 动作 | 说明 |
|---------|---------|------|------|
| | REST / RPC | ✨/🔧 | |

---

## 外部依赖清单

| 服务名称 | 系统归属 | 接口类型 | 依赖场景 | 动作 |
|---------|---------|---------|---------|------|
| | 内部微服务 / 第三方 | REST / FeignClient | | ✨/🔧 |

---

## ✨/🔧 {外部服务名称}

> 🔧 **变更说明**：{仅修改时填写}

### 基本信息

| 属性 | 值 |
|------|---|
| 服务名称 | |
| 系统归属 | 内部微服务 / 第三方平台 |
| 接口类型 | REST（FeignClient）/ RPC（Dubbo） |
| 依赖场景 | {本业务哪些功能需要调用此服务} |

### 接口契约

| 接口 | 方法 | 路径/方法名 | 请求参数 | 响应关键字段 |
|------|------|-----------|---------|------------|
| | GET/POST | | | |

**关键响应字段说明**：

| 字段名 | 类型 | 说明 | 为空时处理 |
|--------|------|------|---------|
| | | | |

### 容错配置

| 配置项 | 值 |
|--------|---|
| connectTimeout | {N}ms |
| readTimeout | {N}ms |
| 重试次数 | {N}（仅超时/5xx 重试，间隔指数退避） |
| 熔断失败率阈值 | 50% |
| 熔断持续时间 | 30s |
| 降级策略 | 返回默认值 / 缓存值 / 跳过 / 抛异常 |
| 降级返回值 | `{具体描述，如 null / 默认对象 / 抛出 APP-SVC-SYS-001}` |

### FeignClient 配置示例

```java
@FeignClient(
    name = "{service-name}",
    fallbackFactory = {ServiceName}FallbackFactory.class,
    configuration = FeignTimeoutConfig.class
)
public interface {ServiceName}Client {
    @GetMapping("/api/v1/{resource}/{id}")
    Result<{ResponseDTO}> get{Resource}(@PathVariable Long id);
}
```

### 依赖场景与影响分析

| 本服务功能 | 调用目标 | 不可用时影响 | 降级方案 |
|---------|---------|-----------|---------|
| {审批流节点分配} | `OrgService.getManager()` | 无法自动分配审批人 | 转为手动选择 |

## 分段：`integration-notification`


**规范分段**：`integration-notification`

### 规范条文

# 消息通知设计规范（TDD 第5章 §5.3）

## 适用场景

本要素覆盖所有**系统向用户发出通知**的设计，包括：
- App Push / 站内信 / WebSocket 实时弹窗
- 邮件通知
- 短信通知
- 企业 IM（钉钉/飞书）通知

## 通知渠道选型

| 渠道 | 适用场景 | 实时性 | 成本 |
|------|---------|--------|------|
| 站内消息（WebSocket）| 应用内实时通知 | 极高 | 低 |
| App Push | 移动端用户离线/在线通知 | 高 | 低 |
| 邮件 | 正式通知、附件传递 | 中 | 低 |
| 短信 | 验证码、紧急告警 | 高 | 中 |
| 企业 IM | 内部流程审批、工作事项 | 高 | 低 |

## 通知模板规范

- 模板变量用 `{variableName}` 占位
- 模板必须支持多语言（`i18n` key 管理）
- 模板变更需要走审核流程（避免随意修改通知文案）
- 区分通知标题和正文，正文限制长度（短信 ≤ 70 字/条）

## 频控策略（防打扰）

- 同一用户同一通知类型在时间窗口内不重复发送（如 10 分钟内不重复推送）
- 频控配置通过动态配置管理（见 config/app-config.md）
- 用户可设置免打扰时段（通知服务统一处理）
- 聚合通知：短时间内多条同类通知合并为一条（如"5 个订单待审批"）

## 解耦设计

- 通知发送应与主业务解耦，通过 MQ 消费状态事件后触发（见 `integration.md` 内 MQ 分节）
- 通知发送失败不影响主业务流程，只记录失败日志并重试
- 通知微服务统一承担所有渠道的发送逻辑，业务服务不直接调用第三方 SDK

## 通知幂等

- 每条通知有唯一 `notificationId`（`{业务ID}_{事件类型}_{时间窗口}`）
- 消费 MQ 时以 `notificationId` 去重，防止重复通知

### 交付模板

# 消息通知设计（TDD 第5章 §5.3）

<!--
变更标注约定：
- ✨ 新增：本次新增的通知场景
- 🔧 修改：渠道/模板/频控策略变更
-->

## 变更概要

| 通知场景 | 渠道 | 动作 | 说明 |
|---------|------|------|------|
| | | ✨/🔧 | |

---

## 通知场景清单

| 通知场景 | 触发事件 | 渠道 | 接收方 | 动作 |
|---------|---------|------|--------|------|
| | | 站内/Push/邮件/短信 | | ✨/🔧 |

---

## ✨/🔧 {通知场景名称}

> 🔧 **变更说明**：{仅修改时填写}

### 基本信息

| 属性 | 值 |
|------|---|
| 触发时机 | {业务事件，如订单状态变更为 APPROVED} |
| 触发来源 | MQ 消费 `{topic}/{tag}` / 直接调用通知服务 |
| 通知渠道 | 站内消息 / App Push / 邮件 / 短信 |
| 接收方 | {申请人 / 审批人 / 管理员等} |
| 通知幂等键 | `{businessId}_{eventType}_{timeWindow}` |

### 通知模板

**标题**：`{通知场景中文名}通知`

**正文模板**：

```
{通知正文，如：您提交的订单 {orderNo} 已通过审批，审批人：{approverName}，时间：{time}}
```

**模板变量**：

| 变量名 | 来源 | 说明 |
|--------|------|------|
| `{orderNo}` | 消息体 | 订单编号 |
| `{approverName}` | 查询用户服务 | 审批人姓名 |
| `{time}` | 消息体 `occurredAt` | 审批时间 |

### 渠道配置

| 渠道 | 实现方式 | 说明 |
|------|---------|------|
| 站内消息 | WebSocket 推送 + 持久化到 `notification` 表 | 用户在线时实时推，离线时展示未读角标 |
| App Push | 第三方 Push SDK（极光/阿里云） | 消息体 ≤ 256 字节 |
| 邮件 | 邮件服务（SMTP / 企业邮件 API） | — |
| 短信 | 短信服务（阿里云 / 腾讯云） | 正文 ≤ 70 字 |

### 频控策略

| 维度 | 时间窗口 | 最大发送次数 | 超限处理 |
|------|---------|-----------|---------|
| 同用户同类型通知 | 10 分钟 | 1 次 | 丢弃（记录日志） |
| 用户免打扰时段 | 22:00~08:00 | 0 次 | 延迟到 08:00 发送 |

### 失败处理

| 场景 | 处理方式 |
|------|---------|
| 发送失败（网络/SDK 异常） | 重试 3 次（指数退避），超限记录失败日志，不影响主业务 |
| 渠道不可用 | 降级到备用渠道（如 Push 失败降级到站内消息） |
